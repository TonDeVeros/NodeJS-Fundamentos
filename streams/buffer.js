// é uma representacao de um espaco da memoria do computador e guarda os dados em binario

const buf = Buffer.from("ok");

console.log(buf);//assim é em hexadecimal

console.log(buf.toJSON());//assim é em decimal
